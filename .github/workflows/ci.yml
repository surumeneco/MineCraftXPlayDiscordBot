name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.17.0

      - name: Install dependencies
        run: npm install

      - name: Verify
        run: npm run verify

      - name: Build container
        run: docker build -t minecraft-xplay-discord-bot:ci .
